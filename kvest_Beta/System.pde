String tex,textFile="text2.txt";
String[] info,n;
void system()
{
  tex="";
  //for(byte i=0;i<loadStrings(textFile).length;i++)
 //   tex+=loadStrings(textFile)[i];
//  info=split(tex,'*');
  textAlign(LEFT);
  textSize(30);
  fill(255,230,200);
 // text(info[0],20,30,730,500);
//  n=int(info[1]);
  for(int i=0;i<n;i++)
  {
  //  button[i].display(textWidth(info[i+2]),0,10,10,info[i+2],info[n+i+2]);
  }
}